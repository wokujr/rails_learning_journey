<div class="d-flex align-items-center justify-content-center">
    <h1> HEY it ROOT and works! </h1>
</div>

<div>
    <!-- print out to the root to check if user successfully logged in/created -->
    <% if @user %>
        Logged in as: <%= @user.email %>
    <%  end %>
</div>
